<%- include("partials/header.ejs") %>

    <!--body section-->
    <div class="postDisplay">

            <div class="postSeeContainer">
                <div class="postChangeContainer">
                  <a class="edit" href="/edit_access/<%= post.postId %>">Edit</a>
                  <a class="delete" href="/delete_access/<%= post.postId %>" >
                    <i class="bi bi-trash trashIcn"></i>
                  </a>
                  
                </div>
                <div class="postTitle"><%= post.title %></div>
                <div class="postDate"><%= post.date %></div>
                <div class="postContent"><%= post.content %></div>
                <div class="postBtnContainer">
                    <div class="author"><%= post.author %></div>
                </div>
            </div>
        
        <!---->

     </div>
     <!-- <script>
        function deletePost(postId) {
            if (confirm("Are you sure you want to delete this post?")) {
                fetch(`/delete/${postId}`, {
                    method: "DELETE"
                })
                .then(response => response.json())
                .then(data => {
                    console.log(data.message); // Log response from server
                    window.location.href = "/"; // Redirect to homepage after deletion
                })
                .catch(error => console.error("Error deleting post:", error));
            }
        }
        </script>
         -->
<%- include("partials/footer.ejs") %>
